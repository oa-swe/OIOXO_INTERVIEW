import { BlockConfig } from "../../types/Config";
import { PossiblePages } from "../../types/Pages";

export const addBlock_config: BlockConfig = {
  possiblePages: [PossiblePages.ALL],
  contents: {
    // Main Container Section
  },
};
